{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom & Prénom</th>
                <td>{{ user.fullName }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('user_index') }}"> <i class="fa fa-lg fa-"></i></a>
    <footer>
        <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-primary pull-right"> <i class="fa fa-edit"></i> Edit</a>
        {{ include('user/_delete_form.html.twig') }}
    </footer>

{% endblock %}
